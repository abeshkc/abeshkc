Modify the app UI and notification behavior with the following changes.

Goal:
Simplify the interface, make Aria’s status clearer, remove Windows notifications, and improve the Aria Insights layout.

1. Add recording status text

When the user starts recording, show a clear secondary status text:

“Press to stop”

Behavior:
- Idle state: show “Click Aria to begin or just type.”
- Recording state: show “Press to stop”
- Processing state: show “Processing...”
- Ready state: show “Review the result below.”

This should appear near the Aria/microphone area and be easy to see.

2. Remove Windows notification feature

Remove or disable Windows system notifications.

The app should no longer trigger Windows toast notifications.

Keep only in-app reminder pop-up dialogs.

Important:
- Do not remove reminder functionality.
- Do not remove in-app reminder alerts.
- Due reminders should still appear inside the app.
- Missed reminder indicators should still work.

3. Remove left overview window

Remove the overview window on the left that shows:
- recent notes
- upcoming reminders

Do not show these overview lists anymore.

Instead, keep only the AI Search feature in that area.

4. Keep AI Search as the left-side feature

The left area should now focus on AI Search only.

Add friendly helper text:

“Click Aria to begin or just type.”

The AI Search area should support:
- typed search
- voice-triggered search
- results display
- clickable results

5. Make “Aria Insights” label bigger and more visible

The label/title currently shown as “Aria Insights” should be larger and clearer.

Suggested styling:
- larger font size
- bold or semi-bold
- left-aligned
- more spacing above/below
- visually prominent section title

6. Set Aria insight update frequency to 1 hour

Change the default periodic update frequency from 10 minutes to 1 hour.

Requirements:
- Default = 60 minutes
- Update settings/UI if there is a frequency selector
- Update documentation
- Update tests

7. Left-align Aria Insights section

Currently Aria Insights appears centered.

Change layout so that:
- the Aria Insights title is left-aligned
- insight cards/bullets are left-aligned
- content starts from the left edge of its panel
- spacing remains clean and modern

Add the text “Click to talk” on Aria’s lower body/avatar area.
Requirements:
- The text should appear directly on or just below Aria’s lower body.
- It should be clearly readable but not too large.
- It should feel integrated into the avatar design, like a small label or badge.
- Keep it visible in idle state.
- During recording, optionally change it to “Listening...”
- During processing, optionally change it to “Thinking...”
- Do not clutter the UI.
- Preserve the existing Aria animation and click-to-speak behavior.

8. Preserve existing features

Do not break:
- local Whisper transcription
- Claude API parsing
- Aria voice interaction
- in-app reminder popups
- missed reminder indicators
- reminders
- recurring reminders
- notes
- meeting notes
- Microsoft sync
- AI search
- sorting
- resizable panes

9. Tests

Add/update tests for:
- “Press to stop” appears while recording
- Windows notification code is disabled/removed
- in-app reminder popup still works
- recent notes overview is removed
- upcoming reminders overview is removed
- AI Search remains visible
- helper text says “Click Aria to begin or just type.”
- Aria Insights title is larger
- Aria Insights content is left-aligned
- default update frequency is 1 hour

10. README update

Update README with:
- in-app reminders only
- no Windows toast notifications
- AI Search-focused left panel
- Aria Insights update frequency set to 1 hour

Implementation rule:
Patch the current app cleanly. Do not rewrite the whole app.