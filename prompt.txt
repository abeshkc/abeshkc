Modify the app layout and voice interaction workflow with the following changes.

Goal:
Improve the voice-first experience and add a dedicated AI search/results area.

1. Rename text below microphone icon

Currently there is text below the microphone icon.

Rename it to exactly:

“Press to talk to Aria”

Requirements:
- centered under the microphone icon
- visually clean and modern
- larger and easier to read
- should fit the new “Your Personal Organizer” branding
- use friendly wording and spacing

Optional:
- subtle fade/pulse animation while idle
- text can change dynamically while recording:
  - “Listening...”
  - “Processing...”
  - “Ready”

2. Split the right-side window into 3 sections

Currently the right-side area contains notes/reminders.

Modify the layout so the right side is vertically split into 3 adjustable sections.

Top section:
- existing Notes area

Middle section:
- existing Reminders area

Bottom section:
- NEW AI Search / Assistant Results area

Use adjustable split panes so users can resize all three sections.

If using PySide/PyQt, use nested QSplitter layouts or equivalent.

3. Add AI Search / Assistant Results section

The new bottom section should support:
- text search
- voice-triggered search
- assistant responses
- intelligent retrieval results

Purpose:
When the user says things like:
- “Can you find my note about Yemen?”
- “Find reminders about DIEM.”
- “Search meeting notes about Lebanon.”
- “Show my urgent reminders.”
- “Find notes mentioning Yaseen.”

the results should appear in this new bottom search/results window.

4. Search area features

The bottom search/results section should include:

- search input box
- microphone icon for voice search
- results list area
- assistant response area if applicable
- loading/progress indicator while searching

The section should support:
- typing search queries
- voice search queries
- displaying matching notes/reminders/meetings
- clickable results
- opening selected items in the editor/view

5. Voice-triggered search behavior

If the user speaks commands beginning with:
- “find”
- “search”
- “show”
- “can you find”
- “look for”

then Aria should classify this as a search intent.

Instead of opening create-reminder forms:
- display results directly in the bottom search/results section.

6. Search result display

Results should display:
- title
- type (note/reminder/meeting)
- date
- importance if available
- source if imported from Microsoft
- small preview/snippet

Clicking a result should:
- open/select the full item in the appropriate section/editor.

7. Search result styling

Style the search/results section to feel like an AI assistant panel.

Possible features:
- assistant-style cards
- grouped results
- highlighted keywords
- assistant summary text like:
  “I found 4 reminders related to DIEM.”

Keep it lightweight and responsive.

8. Preserve existing functionality

Do not remove or break:
- notes
- reminders
- recurring reminders
- reminder popups
- voice transcription
- Claude API parsing
- Microsoft sync
- sorting
- adjustable panes
- confirmation workflow

9. Tests

Add/update tests for:
- “Press to talk to Aria” text appears
- right-side layout splits into 3 sections
- split panes are adjustable
- search section appears correctly
- voice search commands route to search results area
- search results display correctly
- clicking search result opens the correct item
- typing search queries works
- voice search microphone works

10. README update

Update README with:
- new search/results panel
- voice search examples
- AI assistant search workflow
- adjustable 3-pane layout explanation

Implementation rule:
Patch the existing GUI cleanly without rewriting the entire app.
Keep the UI modern, voice-first, and productivity-focused.